# Model & top-level

The high-level entry point. `pychm.Model` wraps the per-model functions; the module-level functions
(`spectrum`, `tuning`, `constraints`) can also be called directly.

::: pychm.Model

::: pychm
    options:
      members: false
